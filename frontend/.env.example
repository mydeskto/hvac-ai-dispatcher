# Optional: point the browser at the API directly instead of the Next.js proxy
# NEXT_PUBLIC_API_URL=http://localhost:4000/api
# Backend the Next.js server proxies /api/* to
BACKEND_URL=http://localhost:4000
